<Page
        class="page"
        loaded="onPageLoaded"
        navigatingTo="navigatingTo"
        xmlns="http://schemas.nativescript.org/tns.xsd">

    <ActionBar class="action-bar">
        <!--
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <!--
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
        <ActionItem icon="res://navigation/menu"
                    android:visibility="collapsed"
                    tap="onDrawerButtonTap"
                    ios.position="left">
        </ActionItem>
        <Label class="action-bar-title">
            <label.formattedText>
                <formattedString>
                    <formattedString.spans>
                        <span text="{{ message }}"/>
                        <span text=" Questions"/>
                    </formattedString.spans>
                </formattedString>
            </label.formattedText>
        </Label>
    </ActionBar>

    <DockLayout stretchLastChild="true">
        <StackLayout dock="top">
        </StackLayout>
        <StackLayout dock="bottom" class="layout-class m-x-2" horizontalAlignment="center" orientation="horizontal">
            <Button class="all t-12 btn btn-primary btn-rounded-lg" text="All" tap="all" width="20%" margin="2dpi" />
            <Button class="correct t-12 btn btn-primary btn-rounded-lg" text="Correct" tap="correct" width="25%" margin="2dpi" />
            <Button class="incorrect t-12 btn btn-primary btn-rounded-lg" text="Wrong" tap="incorrect" width="25%" margin="2dpi" />
            <Button class="skipped t-12 btn btn-primary btn-rounded-lg" text="Skipped" tap="skipped" width="30%" margin="2dpi" />
        </StackLayout>
        <StackLayout visibility="{{ questions.length === 0 ? 'visible' : 'collapsed' }}" class="margin" horizontalAlignment="center">
            <Label textWrap="true" textAlignment="center" marginTop="20dpi">
                <label.formattedText>
                    <formattedString>
                        <formattedString.spans>
                            <span text="No Questions Were "/>
                            <span text="{{ message }}"/>
                        </formattedString.spans>
                    </formattedString>
                </label.formattedText>
            </Label>
        </StackLayout>
        <StackLayout class="margin" visibility="{{ questions.length > 0 ? 'visible' : 'collapsed' }}">
            <ListView id="listView" items="{{ questions }}" separatorColor="blue" itemTemplateSelector="$index">
                <ListView.itemTemplate>
                    <StackLayout>
                        <Label marginTop="20dpi" textAlignment="center" fontWeight="Bold">
                            <Label.formattedText>
                                <FormattedString>
                                    <FormattedString.spans>
                                        <span text="Question " />
                                        <span text="{{ $index + 1 }}"/>
                                        <span text=" Of "/>
                                        <span text="{{$parents['ListView'].questions, $parents['ListView'].questions.length }}" />
                                    </FormattedString.spans>
                                </FormattedString>
                            </Label.formattedText>
                        </Label>
                        <Label text="{{ description }}" textWrap="true"/>
                        <Repeater items="{{ options }}" row="1">
                            <Repeater.itemTemplate>
                                <GridLayout columns="auto,*" class="{{ (!selected && !correct)? 'not-selected m-y-5': (skipped && correct)? 'skipped m-y-5':(selected && !correct)?'incorrect m-y-5': 'correct m-y-5'}}">
                                    <Label col="0" text="{{ tag + '. ' }}"
                                           class="{{ (!selected && !correct)? 'not-selected m-l-2': (skipped && correct)? 'skipped m-l-2':(selected && !correct)?'incorrect m-l-2': 'correct m-l-2'}}"
                                           textWrap="true" marginTop="10dpi" marginBottom="10dpi" />
                                    <Label col="1" text="{{ description }}"
                                           class="{{ (!selected && !correct)? 'not-selected': (skipped && correct)? 'skipped':(selected && !correct)?'incorrect': 'correct'}}"
                                           textWrap="true" marginTop="10dpi" marginBottom="10dpi" />
                                </GridLayout>
                            </Repeater.itemTemplate>
                        </Repeater>

                        <Label text="Explanation: " style="font-weight: bold; margin-top: 10dpi"  textWrap="true" visibility="{{ explanation ? 'visible' : 'collapsed' }}"/>
                        <Label text="{{ explanation }}" textWrap="true" visibility="{{ explanation ? 'visible' : 'collapsed' }}"/>
                    </StackLayout>
                </ListView.itemTemplate>
            </ListView>
        </StackLayout>
    </DockLayout>
</Page>